[pytest]
markers =
    download: tests that download from YouTube (require network)
    slow: tests that take >10 seconds
    whisper: tests that load whisper model (CPU-intensive, very slow on RPi)
testpaths = .
pythonpath = ..